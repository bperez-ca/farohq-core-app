# FaroHQ Core App Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# ============================================
# Database Configuration
# ============================================
# Option 1: Use separate environment variables
DB_HOST=localhost
DB_PORT=5432
DB_NAME=localvisibilityos
DB_USER=postgres
DB_PASSWORD=password
DB_SSLMODE=disable

# Option 2: Use DATABASE_URL (takes precedence if set)
# DATABASE_URL=postgres://user:password@localhost:5432/localvisibilityos?sslmode=disable

# For Cloud Run with Cloud SQL, use Unix socket:
# DB_HOST=/cloudsql/project:region:instance
# DATABASE_URL=postgres://user:password@/dbname?host=/cloudsql/project:region:instance&sslmode=require

# ============================================
# Clerk Authentication
# ============================================
# JWKS URL for JWT token verification
# Format: https://<your-clerk-instance>.clerk.accounts.dev/.well-known/jwks.json
# Find your instance URL in Clerk Dashboard > API Keys
CLERK_JWKS_URL=https://real-pegasus-21.clerk.accounts.dev/.well-known/jwks.json

# Frontend Clerk credentials (for reference - used by portal, not core-app)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# ============================================
# GCP Cloud Storage Configuration
# ============================================
# GCS bucket name for file storage
GCS_BUCKET_NAME=farohq-files

# GCP Project ID (optional, used for some GCS operations)
GCS_PROJECT_ID=

# For local development, set GOOGLE_APPLICATION_CREDENTIALS to service account key path:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# In Cloud Run, Application Default Credentials are automatically used
# No need to set GOOGLE_APPLICATION_CREDENTIALS in production

# ============================================
# Server Configuration
# ============================================
PORT=8080
WEB_URL=http://localhost:3000

# ============================================
# Vercel (Custom Domain Verification)
# ============================================
# Required for domain verification and CNAME instructions (Scale tier)
# Create token at https://vercel.com/account/tokens
VERCEL_API_TOKEN=
VERCEL_PROJECT_ID=
VERCEL_TEAM_ID=

# ============================================
# Production Notes
# ============================================
# In production (Cloud Run), set these via:
# 1. Cloud Run environment variables (for non-sensitive values)
# 2. Secret Manager (for sensitive values like passwords, API keys)
# 3. Cloud SQL connection via Unix socket (automatic with Cloud SQL Proxy)

# Example Cloud Run deployment with secrets:
# gcloud run deploy farohq-core-app \
#   --set-secrets="DB_PASSWORD=db-password:latest,CLERK_JWKS_URL=clerk-jwks-url:latest" \
#   --set-env-vars="DB_HOST=/cloudsql/project:region:instance,DB_NAME=localvisibilityos,GCS_BUCKET_NAME=farohq-files"
